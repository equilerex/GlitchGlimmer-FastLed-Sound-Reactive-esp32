# TODO
<!-- Live working set. `jookoi-paper-trail flush` archives it and resets it. See AGENTS.md. -->

## Context

ESP32 sound-reactive LED firmware (PlatformIO, board `ttgo-t1`, FastLED for LEDs on pins 25/33, TFT_eSPI on 18/19/5/16/23, INMP441 I2S mic on 26/27/32). The codebase was largely AI-generated before current models, and inherited the defects of that era.

`plans/2026-09-16-ai-era-defect-audit.md` holds the audit. `plans/2026-09-16-verification-harness-and-what-it-found.md` holds the current measured state and supersedes parts of the audit, so read it first if you only read one.

The correction matters and is worth stating plainly. The audit called the per-frame layer leak the crash, but `SceneDirector::attachState` had no call sites, so `state` was always null, every director call early-returned, `getActiveScene()` returned null, and the per-strip block that built those layers never ran. The leak was unreachable, and with it the whole scene path. Nothing was drawn at all. So the leak is not the crash cause, and whatever crashed on hardware was not this revision.

The user's real symptom, layers stacking until the animation disappears, came from the compositor instead. Every layer renders additively into a black buffer, and `renderLayers` blended that buffer over the base with `nblend(..., 255)`, where 255 means replace rather than blend. Each layer's unlit pixels therefore erased the base animation. Compositing is now additive with opacity as the layer's share of the light.

Wiring the state also uncovered a crash that had been unreachable for the same reason. `animationCatalog` is sized `AnimationType::COUNT`, which is 9, but only 8 entries were initialised and `NONE` is 0. Every lookup reads one position early, and the last slot was left value-initialised with an empty `std::function` that aborts on call. Any scene built from `ALIEN_PULSE` would have called it. This is the one defect found so far that plausibly explains a crash on hardware, and it only became reachable once scenes actually ran.

Where it stands now. Both environments build and the harness runs 138 checks green. `pio run -e ttgo-t1` succeeds at RAM 8.9 percent and flash 32.6 percent at `-std=gnu++11`. `pio run -e native -t exec` drives the real controller over a test-owned clock, sweeps all eight catalog animations at two lengths and all eight reachable layer factories, soaks the four accumulator sites for 20000 frames each, runs the real FFT through `AudioProcessor`, and round-trips a capture file through the same pipeline the browser writes.

The loop no longer has a throw path. Both history buffers were `std::deque`, which took and returned a 456-byte and a 480-byte node about fifteen times a second, and a failed `push_back` was the one way `loop()` could reach `std::terminate`. They are now `SnapshotRing`, a fixed-capacity ring in `src/audio/SnapshotRing.h`, and `AudioHistoryTracker::addSnapshot` allocates zero times across a whole run. `AudioHistory` is 60000 bytes of heap taken once at boot, which is more than anything reads; `BACKLOG.md` carries the shrink.

Nine further defects are fixed, among them self-transitioning scenes, C++20 designated initializers under `gnu++11`, a function-local static scratch buffer shared across strips, and six unwrapped float phase accumulators. One defect surfaced while fixing those and is recorded rather than fixed: `WormholeVortexLayer` and `CentroidColorFlowLayer` are never instantiated anywhere. Nothing has run on hardware, and that is the open item. It is also worth repeating that the harness's allocation counts exclude `String`, because `sim/stubs/Arduino.h` aliases it to `std::string` and small-string optimisation hides the churn the device would pay.

The repository was then made buildable by someone who is not this machine's owner, and given somewhere to look at the output. Three of the four libraries `src/` includes were reaching the build through a `lib_extra_dirs` absolute path into a global Arduino libraries directory, so a fresh clone found only FastLED. All four are now named in `lib_deps`, which also means the TFT_eSPI on the build machine and the one CI downloads are the same version. TFT_eSPI's display settings followed the same problem one level down, since they lived in the library's own `User_Setup_Select.h` rather than in the repo. `TFT_eSPI.h` checks `__has_include(<tft_setup.h>)` before it reads that file, so `include/tft_setup.h` now carries the panel configuration and wins. Both environments build from a clean tree with nothing but PlatformIO installed.

`web/` has two views over one canvas. The recording view replays frames the harness writes with `--dump-frames`, so its audio timeline is fixed and nothing there runs the FFT. The live view is the same `animations/` and `scenes/` code compiled to WebAssembly by `tools/build-wasm.sh` and stepped once per frame from the page, with the microphone's samples handed to `AudioProcessor` unchanged, so the FFT, the feature extraction, the beat detector and the mood classifier are the firmware's own. It opens on the microphone and falls back to a synthetic signal when the browser will not grant one. `?debug=1` turns on a console trace of every scene and mood change with its dwell time, retained on `window.ggTrace`.

The live view reads its state back out of the module every frame and shows it: the mood now, the mood of the averaged window, the scene clock against its own thresholds, and one bar per classifier input with the threshold marked on it. That panel is what turned "the animations keep blacking out" from an impression into a number. Before the feature-scale fix, the synthetic signal left strip 0's summed channels under 2000 of a possible 76500, lit at around two percent. The cause was the feature scale, not the compositor: `energy` is a raw FFT magnitude sum and five separate comparisons treated it as 0..1, and the bass, mid and treble divisors were small enough that broadband audio pinned all three at 1.0. With the sweep done the same panel reads all 100 pixels of strip 0 non-black at 5600 to 9500 summed channels, so roughly seven to twelve percent of the strip's capacity rather than two.

That scale problem is now fixed at the producer. `AudioFeatures::level` is the block's volume as a fraction of the loudest block in the last few seconds, so it is 0..1 at any microphone gain and needs no device constant, and `MoodHistory::classifyMood`, the classifier copy in `SceneRegistry::pickSceneByMood`, `SceneState::shouldTransition` and `calculateIdealDuration` all read it. The gate's thresholds became multiples of the noise floor for the same reason, since a fixed threshold was a claim about one microphone's output and this one runs about 40 dB below it, which left the gate shut through music. `AlienPulse` and `BassPulseStorm` were rendering black off the same class of constant and now drive off `level`. `bassHits` was never assigned anywhere in the repo, so three consumer branches were dead.

The producer fix was not enough on its own, because the consumers were still comparing against absolute magnitudes. Every remaining `.energy`, `.volume` and `.loudness` read in `src/` was reviewed and the comparisons moved onto `level`: `NeonBeatTunnelAnimation` was black because it multiplied a 0..255 wave by a volume near 0.008, `PsychedelicInkSquirtAnimation` had an `energy * 0.05` term contributing 5 to 50 units a second against two musical terms of about 2, `EnergyPulseRiverLayer` moved a river hundreds of pixels a frame, `EnergyFogLayer` sat at its brightness ceiling on every frame, `MoodMemoryArcLayer` and `CentroidColorFlowLayer` read the same wrong scale, and `SceneDirector::maybeInjectReactiveLayer` had a `> 0.6f` test that was true on every frame and so injected an overlay on its 1500 ms timer regardless of the audio. `DisplayManager`'s power bar and power readout were reading `loudness`, which is `volume * 100`, so both showed zero through music. `MoodReactiveAnimation.h` was dead and unbuildable, referencing mood types that do not exist, and is deleted. The remaining absolute uses are the producer, the snapshot plumbing and the harness, none of which compares one against a threshold.

Two defects were fixed on the strength of that same review. `SceneState::shouldTransition` compared the instantaneous `level` against the snapshot taken when the scene began, and `level` is renormalised to the loudest recent block with about 11 percent per-frame churn, so the 0.25 delta was crossed within a second or two of any real audio and `pastMin && moodShift` was satisfied the moment the minimum elapsed. Every scene therefore changed at its minimum duration, which is the reported bug. It now compares the classified mood, which is already smoothed, confirmed and held, and the bpm term is unchanged. Second, the mood's fixed thresholds were a claim about one input's scale: on the microphone in use `dynamics` lives between about 0.46 and 0.57, so `> 0.5` was true on nearly every frame and `< 0.2` never. The cut points are now 30 and 70 percent of the range the classifier has measured, with the dynamics clause dropped entirely when that range is too narrow to split. A mood also now holds for a minimum dwell, and a competing mood must persist for a confirmation window before it may replace it, with the UNKNOWN to real transition exempt from both, since a held mood to outlast exists only once a real one has been chosen.

All of that is adjustable live. The page's Tuning group drives eight dials over one indexed wasm pair: the scene clock's minimum base, ideal base and ideal span, and the mood's minimum hold, confirmation window, smoothing rate and two dynamics-window rates. They persist to `localStorage` and have a reset. The two dynamics-window rates are per second rather than per frame, so the same window adapts at the same wall-clock speed on the device's 33 ms loop and the page's per-frame loop. The panel's dynamics markers are placed from the firmware's own cut points rather than a constant, and hidden when the measured range is too narrow to split.

The loop for tuning against the real microphone is `Record` on the live page, which writes what the page fed the analyser to a `.f32` file, then `pio run -e native -t exec -- --replay <file>`, which feeds it back through the real `AudioProcessor` and reports the min, max, mean and frame-to-frame churn of every feature plus the mood dwell times. The churn column is what separates a value inside a sane range from one inside a sane range and unusable, and the format is verified end to end by `?selftest=1` on the page against `checkReplay` in the harness. The browser is still the only place the microphone is reachable, so this is the only route from a real input to a measurement.

`tools/build-wasm.sh` compiles to cached objects under `web/live/.obj` and rebuilds in about 20 seconds after a source or repo-header change, against about 85 seconds cold. Pass `--watch` to rebuild on save. The residual cost is the link of 135 objects, not the compile: a no-change run still takes 15 seconds. Archiving FastLED's objects so the linker pulls only what is referenced was tried and made it slower, so it is not in the script.

The animations were still reading near black at the microphone's level after the feature-scale fix, and the cause turned out to be in FastLED rather than here. `hsv2rgb_rainbow` squares `CHSV`'s `val` before it scales the channels, `val = scale8_video(val, val)`, so `CHSV(h, s, v)` emits duty proportional to `v * v`. A `sqrt` brightness curve on that input cancels exactly, and the catalog animations measured at a ratio of exactly `level` against a full-level run, which is that quadratic response written as one number. `AudioFeatures` now carries a curve per colour sink: `pixelLevel()` for brightness written as a scale on `CRGB`, and `hsvLevel()` for brightness written through `CHSV`'s `val`, which supplies the other half of the exponent so both sinks land on the same emitted duty. The tunnel's brightest channel at the microphone's level went from 40 to 98 of 255 and its measured ratio from 0.162 to 0.399, which is `sqrt(0.159)` to three places. A harness check measures the conversion itself rather than the arithmetic, so a FastLED upgrade that changes the squaring fails there instead of as a strip that has quietly gone dark.

`level` was the other half of that report, and it was not a level. It was the current block over the loudest recent one with nothing after it, so any block louder than the one before set it to 1 for a single frame and the value fell away again by the next. At roughly 86 blocks a second that is a spike train, and it is what the readout was showing. It is now normalised against the noise floor as well as the peak, so silence reads as zero by construction rather than as a ratio of two falling terms that climbs back toward 1 and was only hidden by the gate, and it is followed by an asymmetric one-pole filter with separate attack and release. The mood thresholds were tuned against the unfiltered value and still pass, because the classifier smooths, confirms and holds the mood before acting on it.

## Checklist

- [x] Build graphify knowledge graph (`graphify-out/`)
- [x] Audit animations and layer lifecycle
- [x] Audit audio and mic pipeline
- [x] Audit display and blocking paths
- [x] Set up paper trail
- [x] Write the fix plan from the audit findings
- [x] Fix the layer leak — layers rebuilt on scene change, not per frame (`LEDStripController.h`, `LayerManager.cpp`)
- [x] Fix the display cost — no per-frame `fillScreen`, no 4-widget cap, no internal 500ms clear (`DisplayManager.cpp`, `GridLayout.h`)
- [x] Fix the double ownership of widgets (`GridLayout` is now non-owning)
- [x] Restore CPU to 240 MHz (`src/main.ino`)
- [x] Fix `ArduinoFFT<float>` and drain the I2S ring every pass (`AudioProcessor.cpp`, `MainController.cpp`)
- [x] Assign `AudioFeatures::average` and `dynamics` — both were computed then dropped, leaving them permanently 0
- [x] Instantiate `SceneState` and attach it — without this the entire scene path was dead code
- [x] Make layer compositing additive, so a layer can no longer erase the base it sits on (`LayerManager.cpp`)
- [x] Enforce the layer cap at insertion, matching what actually renders — render capped at 3 while update ran all of them
- [x] Drop the outer fade in `LEDStripController::update` — it reached only strip 0 and decayed it twice per frame
- [x] Give scenes a field + accent preset and an accent opacity for the shockwave (`SceneRegistry.cpp`, `VisualLayers.h`)
- [x] Point the display's scene name at the live director instead of a second, unstateful one (`MainController.cpp`)
- [x] Align `animationCatalog` with `AnimationType` — `NONE = 0` left every lookup one slot early and the trailing empty `create` aborts when called (`AnimationCatalog.cpp`, `SceneRegistry.cpp`)
- [x] Build it (`pio run`) and confirm it compiles — both environments build, `[env:ttgo-t1]` at RAM 8.3% and flash 32.7%, and `[env:native]`
- [x] Add a host harness for the scene and layer lifecycle — real controller, stub Arduino core, a clock the test owns, both strips drawn to the terminal (`sim/stubs/Arduino.h`, `src/sim_main.cpp`)
- [x] Run the harness green — 14 of 14 checks across 3000 simulated frames, with the leak measured rather than argued
- [x] Narrow `lib_ldf_mode` back to the default once `lib_compat_mode = off` carried the load — native build 46 s to 21 s
- [x] Remove the loop's throw path — both `std::deque` histories to a fixed-capacity `SnapshotRing`, so `addSnapshot` allocates zero times per run (`src/audio/SnapshotRing.h`, `AudioHistoryTracker.h`, `MoodHistory.h`)
- [x] Drop the per-frame `String` work — cache the scene name on transition, compute the widget's digit count arithmetically (`MainController.cpp`, `Widget.h`)
- [x] Remove the misplaced `try`/`catch` around non-throwing calls and add a backstop `catch` in `loop()` (`LayerManager.cpp`, `main.ino`)
- [x] Fix the verified defects — self-transitioning scenes, dead `SceneDefinition::activeScene`, C++20 designated initializers under `gnu++11`, the function-local static layer buffer, the `neonFlow` brightness underflow, the negative `uint8_t` argument in `AlienPulse`, six unwrapped phase accumulators, zero-length guards in all eight catalog animations, and a heap monitor with no call sites
- [x] Sweep every animation and layer — eight catalog animations at n=100 and n=8, eight layer factories, device-scale audio with a populated spectrum and waveform, and a per-animation runaway budget
- [x] Soak the accumulators — 20000 frames per site, asserting each stays inside one period via a test seam, since precision exhaustion itself is not reachable in a harness
- [x] Set `default_envs` and document both build commands, stating that neither needs a board attached (`platformio.ini`)
- [x] Make the build independent of this machine — name all four libraries in `lib_deps` and move the TFT_eSPI panel config into `include/tft_setup.h`, so `lib_extra_dirs` is gone
- [x] Record frames from the real firmware and replay them in a browser — `--dump-frames` in `sim_main.cpp`, and `web/` as a dependency-free player
- [x] Verify the player rather than assuming it — headless `--screenshot` does not capture canvas content, so the check is a `getImageData` readback plus a `toDataURL` dump decoded to PNG
- [x] Run both environments in CI, since the device build is the only thing that compiles at `-std=gnu++11` and catches newer constructs reaching device code
- [x] Prove portability from a bare clone rather than by cleaning `.pio` — cloned to a different path, built both environments with nothing but PlatformIO and a host compiler, and the frames it recorded hash byte-identical to this machine's
- [x] Replace the README, which still opened with "not functional", and drop the stock PlatformIO placeholder files under `include/`, `lib/` and `test/`
- [x] Put a captured frame in the README (`docs/preview.png`), since the visualisation section described a page that needs a build before it can be seen
- [x] Compile the real pipeline to WebAssembly and drive it from the browser's microphone — `extern "C"` on the exported surface, since `EMSCRIPTEN_KEEPALIVE` alone leaves the names C++-mangled and the page sees undefined functions (`src/wasm_main.cpp`, `tools/build-wasm.sh`, `web/live.js`)
- [x] Fix `AudioProcessor::analyzeAudio` computing `volume`, `peak` and `loudness` into members and never copying them into the returned struct — all three stayed 0, so every animation reading them was dead, and the native env never compiled the file so nothing caught it (`AudioProcessor.cpp`)
- [x] Cover the real FFT in the harness — every other check hand-builds `AudioFeatures`, which is why the uncopied fields survived 101 checks (`src/sim_main.cpp`)
- [x] Open the live view on the microphone — with a guard on `AudioContext.state`, because a context that is still suspended returns silent buffers and an all-black strip reads as a rendering bug rather than a pending permission (`web/live.js`, `web/main.js`)
- [x] Show the firmware's own state in the interface — mood now against the mood of the averaged window, the scene clock against its own thresholds, and one bar per classifier input with the classifier's threshold marked on it (`web/live.js`, `web/index.html`, `web/style.css`)
- [x] Add console telemetry of every scene and mood change with its dwell time, behind `?debug=1` (`web/telemetry.js`)
- [x] Normalize the audio feature scale at the producer — `AudioFeatures::level` is the block's volume over the loudest recent block, so it is 0..1 at any gain, and the classifier, its copy in `pickSceneByMood`, and the scene clock all read it (`AudioFeatures.h`, `AudioProcessor.cpp`, `MoodHistory.h`, `SceneRegistry.cpp`, `SceneState.cpp`)
- [x] Fix the animations that hardcoded a device-scale constant against an unnormalized field — `AlienPulse`'s `powf(energy / 1800.0f, 1.5f)` and `BassPulseStorm`'s `bass * 255 + peak * 128` both drove the strip to black, and `bassHits` was never assigned so three consumer branches were dead (`AlienPulse.h`, `BassPulseStormAnimation.h`, `AudioProcessor.cpp`)
- [x] Fix the silence gate — its thresholds were fixed amounts above the floor, which is a claim about one microphone's output, and this one runs about 40 dB below it, so the gate stayed shut through music (`AudioProcessor.cpp`)
- [x] Make the spectrum panel readable — one running peak for the whole row pinned the first five bars at 100 percent, and it now holds one reference per bar and scales with the gate (`web/render.js`)
- [x] Build the tuning loop — `Record` on the live page writes what it fed the analyser to a `.f32`, and `--replay <file>` in the harness reports min, max, mean and frame-to-frame churn per feature plus mood dwell times, verified end to end against the page's `?selftest=1` mode (`web/live.js`, `src/sim_main.cpp`)
- [x] Make the wasm build incremental and watchable — cached objects with separate repo and library header stamps, about 20 seconds per change against about 85 cold, and `--watch` to rebuild on save (`tools/build-wasm.sh`)
- [x] Convert the animations still reading an absolute magnitude — every `.energy`, `.volume` and `.loudness` comparison in `src/` moved onto `level`, across `NeonBeatTunnelAnimation`, `PsychedelicInkSquirtAnimation`, `VisualLayers`, `SceneDirector` and `DisplayManager`, and the dead unbuildable `MoodReactiveAnimation.h` is deleted
- [x] Hold a mood for a minimum dwell before the classifier may leave it, and move each mood's thresholds with the signal rather than fixing them — `dynamics` cut points are 30 and 70 percent of the measured range or dropped when the range cannot be split (`MoodHistory.h`)
- [x] Stop the scene clock changing at its minimum duration — `shouldTransition` compared an instantaneous renormalised `level` against the scene-start snapshot, so the delta was crossed within a second or two of any audio; it now compares the classified mood (`SceneState.cpp`)
- [x] Put the scene and mood timing on live controls — eight dials over one indexed wasm pair, persisted to `localStorage`, recomputing the running scene's own thresholds rather than the next one's (`web/live.js`, `src/wasm_main.cpp`, `src/core/LEDStripController.h`)
- [x] Place the panel's dynamics markers from the firmware's cut points rather than a constant, and hide them when the range is too narrow to split (`web/live.js`)
- [x] Curve brightness per colour sink — `CHSV`'s `val` is squared inside `hsv2rgb_rainbow`, so the `sqrt` curve on that input cancelled exactly and every catalog animation still rendered near black; `AudioFeatures::hsvLevel()` supplies the other half of the exponent, and a check measures the conversion so a library change that alters the squaring fails there (`AudioFeatures.h`, `Config.h`, `NeonBeatTunnelAnimation.h`, `BassPulseStormAnimation.h`, `neonFlow.h`, `PsychedelicInkSquirtAnimation.h`, `VisualLayers.h`, `sim_main.cpp`)
- [x] Make `level` a level rather than a spike train — normalised against the noise floor as well as the peak so silence reads as zero by construction, then followed by an asymmetric one-pole filter with separate attack and release (`AudioProcessor.cpp`, `Config.h`)
- [ ] Re-verify the fixed feature scale against a real microphone, not the synthetic signal, using a capture from the live page
- [ ] Enable Pages once in the repository settings, Source set to GitHub Actions, for `.github/workflows/pages.yml` to publish
- [ ] Flash and confirm on hardware: strips animate at all, no crash under scene changes, stable heap, screen free of flicker, mic responsive
- [ ] Write `ARCHITECTURE.md` — deferred until the loop is confirmed working on hardware
