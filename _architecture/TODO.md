# TODO
<!-- Live working set. `jookoi-paper-trail flush` archives it and resets it. See AGENTS.md. -->

## Demo build tooling note — 2026-09-21

`tools/run-native.js` must not assume the `pio` launcher is on `PATH`. It probes
`pio`, `python -m platformio`, and `py -m platformio` in that order on Windows
(with the Python 3 variants on POSIX). This matters for IDE-triggered pre-push
hooks, where PlatformIO may be installed in Python while its script directory
is absent from the hook's shell `PATH`.

`tools/dump-frames.js` repeats the WinLibs runtime-path lookup for the second
process boundary. The PlatformIO build can succeed while `program.exe` still
fails to start if Git Bash or another MinGW installation wins `PATH`; frame
generation needs the same compiler `bin` directory at runtime as the linker.

## Context

ESP32 sound-reactive LED firmware. PlatformIO, board `ttgo-t1`, FastLED on pins 25/33, TFT_eSPI on 18/19/5/16/23, INMP441 I2S mic on 26/27/32. `src/` is also the audio analysis and the animation library, and three entry points drive it: `src/main.ino` on the board, `src/sim_main.cpp` as the host harness, `src/wasm_main.cpp` in the browser.

`ARCHITECTURE.md` holds the shape and the load-bearing decisions, and the previous Context is in `archive/2026-09.md`. Read `ARCHITECTURE.md` before touching the audio feature model, the compositor or the brightness curves, because most of what looks like a tuning constant in those places is a rule with a reason behind it.

Where this stands. Steps 0–3 of the music-engine rebuild are complete. All 8 MusicState coordinates, confidence scores, trends, structural flags, and sample-clock metrics are now exported across WASM, the browser telemetry deck/HUD, and the native harness. Both the device C++11 build (Flash 25.2%, RAM 9.1%) and the WASM build succeed cleanly. Uncommitted worktree. Nothing has run on hardware, so do not describe the firmware as working.

### Handoff state — 2026-09-21

- The active handoff is `plans/2026-09-21-animation-engine-and-selection-HANDOFF.md`.
- Completed: Music engine rebuild Steps 0–3, autocorrelation rhythm tracking with beat phase & confidence, silence gate hangover and gain-invariant novelty, and real audio song demo mode with custom file loading.
- Step 4 completed: `BeatClock.h` (beat phase with a free-running fallback, plus `TensionRamp`, `HoldLatch`, `HoldSelect`); five rhythmic animations on it (Heartbeat, Beat Scanner, Gentle Pulse Wave, Color Slam, Neon Beat Tunnel); Rising Tension, Strobe Pulse and Pop Fade following `f.buildup` instead of a timer; Lava Cyber Storm, Space Wizards, Playa Chaos and Hybrid following the coordinates instead of a 6-10 s timer; `AnimationProfile.h` (7-axis music-space profile per animation, `profileDistance`); `SceneRegistry::pickSceneByMusic` and `sceneDistance`; a challenger margin plus dwell in `SceneDirector::update`, with the recent-scene ring in `SceneState`; structural events and beat accents in `maybeInjectReactiveLayer`; 20 total ports from Serenity themes (55 active animations in catalog); coordinate inputs hooked into Alien Breath, Bass Pulse Storm, Twilight Ripple, Aurora, and Neon Flow; member buffer `tempBuf` replacing `CRGB temp[n]` stack array in `MultiLayeredHybridAnimation`; regression test for `LEVEL_REF_MIN_OVER_NOISE` added to `sim_main.cpp`.
- Layer system overhaul completed: identified why NoiseFloorMistLayer, TriwaveBeatLayer and MoodMemoryArcLayer were insistent. `SceneRegistry::layersForIntensity` attached permanent `durMs=0` scene layers using a coarse 1D intensity ladder (<0.30 gave BACKGROUND+MOOD_ARC, <0.50 gave BACKGROUND+TRIWAVE_BEAT). Meanwhile `LEDStripController::update` never called `sceneDirector.maybeInjectReactiveLayer(strips[i].layers(), audio, now)` so transient reactive layers never fired. Fixed by wiring reactive injection into the controller, cleaning `layersForIntensity` so quiet scenes are clean, and retuning layer render routines (`NoiseFloorMistLayer`, `TriwaveBeatLayer`, `MoodMemoryArcLayer`) with proper opacities and level/beat/gate attenuations.
- Remaining in Step 4: tune profiles on real audio; foreground browser verification of scene selection (`http://127.0.0.1:8000/?source=demo`).
- All environments green: native harness 491/491, node tests 32/32, WASM build clean, device esp32s3 build clean (Flash 25.8%, RAM 9.4%). Live behaviour of the new selector is unverified: check `http://127.0.0.1:8000/?source=demo` in a foreground tab.
- Where the reasoning lives: the selector's design and rejected alternatives are `plans/decisions/001-select-scenes-by-distance-in-music-space.md`; `ARCHITECTURE.md` has a `Scene selection by music` section; `src/scenes/CONTEXT.md` and `src/animations/CONTEXT.md` cover the code. `level` has a floor at eight times the noise floor (`LEVEL_REF_MIN_OVER_NOISE`) with a dedicated regression test in `sim_main.cpp`. What `level` should mean for a steady song is an open decision in `BACKLOG.md`.
- Observability and web telemetry:
  - WASM bridge exposes coordinates, confidences, trends, and sample clocks via `gg_feature(23..49)` and dedicated getters (`gg_sample_frame`, `gg_sample_time_ms`, `gg_dt_seconds`). `gg_reset_analysis()` resets `g_last` alongside the processor and mood history. Exported `beatPhase` (feature 50, `gg_beat_phase`) and `beatConfidence` (feature 51, `gg_beat_confidence`).
  - Native `kTracked` (`src/sim_main.cpp`) tracks all 8 coordinates, confidences, trends, `beatPhase`, and `beatConfidence` for `--replay` and `--trace` CSV logging.
  - Web UI cockpit and diagnostics deck:
    1. A persistent `Music Coordinates (8D)` card displaying live percentage meters, confidence-weighted opacity, dynamic trend indicators (↑/↓/→), reactive event badges (DROP, TEASE, BUILDUP, DESCENT, ANOMALY), and sample clock hop timing (`dt` in ms and continuous frame index).
    2. A `Tempo & Beat` card displaying median BPM, a continuous 0..100% beat phase progress meter (color-coded by confidence: green for confident lock, cyan for acquisition), phase & confidence statistics, dynamic range, and Schmitt silence gate state.
    3. A `Live Event Stream` feed capturing real-time detector pulses and state changes with sample-clock timestamps. Gate transitions are debounced (requiring 1.5s silence to close, 300ms signal to open, 2s cooldown) to eliminate gate flapping spam; includes a `Gate: Shown/Hidden` filter button, `Clear` history button, scrollable `max-height` viewport, and expanded 100-event buffer.
    4. Comprehensive tooltips on all telemetry cards, badges, and coordinate items in `web/index.html`, `web/main.js` (`coordTooltip`), and `web/state.js` (`COORD_INFO`), providing explicit base data sources (buffer sizes, sample rates, bin counts), mathematical formulas, and musical meanings. The coordinate card now displays full names (`Intensity`, `Activity`, `Brightness`, `Weight`, `Pulse regularity`, `Tempo`, `Texture`, `Presence`) instead of three-letter abbreviations, spells out the sample interval/frame readout, and supports keyboard focus for tooltip access.
    5. Comprehensive `note:` documentation across all rows in `STATE_GROUPS` (`web/live.js`) covering classifier inputs, silence gate, frequency bands, structural detectors, all 24 coordinate values/confidences/trends, and sample clock timing.
    6. Scene Freeze & Animation Isolation Tool: Added an interactive freeze toggle (`[🔓 Auto]` / `[🔒 Frozen]`), metadata pill (`[Mood · Role]`), and categorized `<optgroup>` select dropdown (`💥 Structural Moments`, `🔥 High Energy & Groove`, `🌿 Ambient & Chill`) in the top-left cockpit item (`web/index.html`, `web/style.css`, `web/live.js`, `web/main.js`). Displays associated mood, role (Bed / Rhythm / Event), and target intensity for every animation. Allows locking the scene to any animation in the 44-entry catalog for testing animations and reactive layer injections in isolation without auto-transitions, while preserving full music feature processing and audio reactivity. Backed by C++ methods in `SceneDirector`, `SceneRegistry`, `LEDStripController`, and WASM exports (`gg_lock_scene`, `gg_unlock_scene`, `gg_scene_count`, `gg_scene_name_by_index`, `gg_scene_mood_by_index`, `gg_scene_role_by_index`, `gg_scene_intensity_by_index`, `gg_locked_scene`, `gg_current_scene_index`).
    7. Photometric LED Profiles & Room Glow Tuning: Retuned visual profiles per hardware specs: WS2812B (`smd`, `sil`) brightness 2.0x and glow 1.5x; Pebble (`peb*`) and Fairy Pip (`fairy`) brightness 2.0x and glow 2.0x; COB and FCOB brightness 1.3x, exposure -0.6 EV, and glow 1.2x. Base/fallback hardware parameters remain untouched at 1.0x. Added dedicated `Room glow size` slider (`s.hw.roomGlow`, default 2.5x, range 0.5x–5.0x) to scale ambient room diffusion downsampling and blur footprint independently of Room spill alpha (`web/viz/StripView.js`, `web/viz/hwStore.js`, `web/state.js`, `web/index.html`).
- Verification status:
  - Frontend unit tests: `32 of 32` passed (`npm test`).
  - WebAssembly: `npm run wasm` builds cleanly (Emscripten 6.0.9).
  - Device build: `pio run -e esp32s3` succeeds serially (`-std=gnu++11`, Flash 25.8%, RAM 9.4%).
  - Native harness: `491 of 491` checks passed (`npm run native`).
  - Real microphone capture `docs/glitchglimmer-2026-09-17T20-27-18-691Z.f32` is available for offline replay and served browser testing.

`web/` was rebuilt as an instrument panel in ten reviewed tasks, per `plans/2026-09-18-led-strip-visualizer.md`. **Read `web/viz/CONTEXT.md` before touching the renderer** — it carries the four-input model and the two rules most easily broken by a well-meaning change. The two that cost the most to rediscover: continuity is emergent through `fuses()` (`sigma > pitch`) and must never become a per-type branch in the lit path, which was already caught once in review; and `app.js` and `live.js` each construct their own `StripView` on one canvas, so binding is repeatable and the stale view must be detached or its pointer handlers corrupt the pose. `npm test` runs 26 checks on `node --test`, which raised the Node floor to 22 because it no longer accepts a directory argument. The approved design lives in the repo at `web/design/mockup-reference.html`; the page follows its language, not its content. The browser's live WASM renderer now separates fixed device capacity from runtime software length/count; length and pitch are software controls, while physical board macros remain unchanged.

Three things a cold session would otherwise re-derive. The only real microphone capture is `docs/glitchglimmer-2026-09-17T20-27-18-691Z.f32` and it is gitignored, 2.3 seconds long, which is shorter than the windows the measurements now use. The harness's allocation counts exclude `String`, because `sim/stubs/Arduino.h` aliases it to `std::string` and small-string optimisation hides the per-frame churn the device would pay. And the browser's pixel counts come from `Config.h` live but from `web/data/manifest.json` for recordings — they agree by convention only, which is logged in `BACKLOG.md`.

Open questions that are the owner's call rather than defects, and the visualiser's deferred follow-ups, are logged in `BACKLOG.md`.

### Demo distribution and Pages — 2026-09-21

- The browser demo ships as committed `dist/`, containing the static page, the
  native-harness frame recording, and the WebAssembly module. A fresh clone can
  serve it with `npm start` without installing PlatformIO or Emscripten.
- `npm run build:dist` is the only refresh command. It builds the native target
  first (`pio run -e native` through `tools/run-native.js`), runs the resulting
  executable through `tools/dump-frames.js`, builds WASM through the pinned SDK,
  and copies `web/` to `dist/`. The native build is not optional: frame data is
  produced by the harness executable.
- `.githooks/pre-push` runs the refresh command and rejects the push if the
  generated `dist/` differs afterward. Hooks are opt-in per clone; install with
  `npm run setup:hooks`. The hook never creates or amends a commit silently.
- `.github/workflows/pages.yml` only uploads and deploys `dist/`. It no longer
  spends a hosted runner rebuilding native and WASM artifacts on every push.
- The native host flags must remain portable. Do not add unconditional
  `-lws2_32`: that Windows-only Winsock library breaks the Linux CI runner, and
  the harness does not use Winsock.

## Checklist

- [x] Sweep and overhaul the layer injection and lifecycle system. Resolved permanent layer bloat where all 4 layers were stuck permanently for 20s at a time. Base scenes now attach conservative layer sets (0-2 layers max per `layersForIntensity`), and `SceneDirector::maybeInjectReactiveLayer` enforces finite durations (450ms–3500ms) with cooldowns and automatic expiry, allowing visual layers to pop in dynamically on drops, buildups, and high-confidence beats without suffocating the strip.
- [x] Fix live telemetry discrepancies:
  1. Input attenuation: added `analyserGainNode` (gain 0.12) to match INMP441 microphone dynamic range (~0.035 RMS), preventing `energy` and `level` from pinning at 100%.
  2. Composite intensity: updated `AudioProcessor` to derive intensity from composite `0.40 * level + 0.30 * activity + 0.20 * bassLevel + 0.10 * dynamics` matching model research, preventing chill tracks from reading 90% intensity.
  3. Buildup & descent tracking: added 350ms hangover hold timer bridging beat troughs so structural build/descent signals stay active and fire reliably without zeroing out on transient beat valleys.
  4. Diagnostics & observability: added compact token-efficient markdown clipboard exporter (`📋 Copy Diagnostics`), relative "seconds since last seen" readouts on structural badges/events, and dynamic energy auto-scaling.
- [ ] Finish Step 4 per the handoff: remaining themes ports, coordinate inputs for the animations that still ignore them, profile tuning on real audio, and a foreground-browser pass over the selector (`?source=demo`). The web `Mood State` item is now `Structure` and shows only the structural mood or `Steady`; `MoodHistory` still supplies those structural moods and the legacy fallback for hand-built snapshots, so it is not removable yet.
- [x] Fix the web panel: `coordTooltip`, `coordLabel`, `visibleEvents` and `clearEvents` were referenced by the template and never defined, which threw on render, and the coordinate grid, event stream and event badges had no CSS (`web/main.js`, `web/style.css`, `web/index.html`).

- [x] Execute the music-engine rebuild through steps 0–3 from `plans/2026-09-21-music-engine-rebuild-HANDOFF.md`. Implementation, native runtime harness (`364/364` checks), serialized device C++11 build, trace tooling and serialized WASM artifact build are complete in the uncommitted worktree. Stop here and report back before selection policy, animation metadata, hysteresis or layers (step 4).

- [x] Implement robust rhythm tracking via autocorrelation periodicity analysis, continuous phase tracking, and perceptual tempo bias (Option 3). Fixes octave jumping (e.g. 116 BPM misidentified as 199 BPM due to 250ms refractory cutoff locking onto 8th-note off-beats) by extending refractory lockout `MIN_BEAT_INTERVAL` to 320ms, increasing `BEAT_BASS_RISE` to 1.30, evaluating autocorrelation across 192-frame (~2.2s) onset novelty window with 120 BPM Gaussian prior, accumulating continuous fractional beat phase with PLL onset nudging, and exposing `beatPhase` and `beatConfidence` through C++, WASM, HUD, and native traces (`src/config/Config.h`, `src/audio/AudioProcessor.h`, `src/audio/AudioProcessor.cpp`, `src/audio/AudioFeatures.h`, `src/wasm_main.cpp`, `web/live.js`, `web/index.html`, `web/state.js`).

- [x] Fix low-volume beat dropouts, bright-track tempo halving, and spectrum flashing.
  1. Silence gate hangover: added `GATE_HANGOVER_MS = 350` ms to `AudioProcessor::analyzeAudio` hysteresis so pauses between beats, vocal phrases, and synthetic pulse troughs in Demo mode do not flap `signalPresence` on and off.
  2. Spectrum canvas flashing: in `web/live.js`, switched `spectrum.paint(..., f.gateGain)` from `f.presence * f.level` which had crushed quiet audio and blanked the canvas on binary presence drops.
  3. Gain-normalized multi-band novelty: normalized spectral flux, bass novelty, and mid novelty by each block's energy (`bSum`, `mSum`, `eTotal`), keeping onset signal-to-noise ratio identical regardless of microphone distance or track volume.
  4. Multi-band beat triggers: added mid/snare backbeat triggers (`normMidFlux > 0.25`) and phase-locked beat triggers (`beatConfidence >= 0.35`) alongside bass kicks, and lowered `BEAT_MIN_BASS_LEVEL` to `0.15f` so bright acoustic and pop tracks register regular beats instead of dropping to 30–60 BPM.
  5. Autocorrelation octave disambiguation: added half-lag harmonic check (`halfLag = (bestLag + 1) / 2`) when `bestLag >= 52` (sub-100 BPM), locking onto fundamental quarter-note tempos when 2-beat kick patterns produce bar-level subharmonics. Autocorrelation now actively locks `currentBPM`.
- [x] Enable real song sampling for Demo mode and user audio loading in browser visualizer.
  1. Bundled sample audio tracks: added `web/audio/demo.mp3` (EDM Beat at 128 BPM) and `web/audio/jazz.mp3` (Jazzy Percussion) in repository, with `.mp3`, `.ogg`, `.wav`, `.m4a`, and `.flac` MIME types supported in `tools/serve-web.js`.
  2. Web Audio player engine: in `web/live.js`, implemented `AudioBufferSourceNode` playback through `analyser` (for FFT, spectrum, and LED animation drives) and `audioGainNode` connected to `audioContext.destination` (for audible speaker playback).
  3. Interactive UI & Controls: in `web/index.html`, added a three-way source selector (`Microphone`, `Demo Track`, `Synth Tone`), plus an interactive audio player bar for Demo mode with `Play/Pause`, `Sound On/Muted` (visuals continue reacting silently when muted), track switcher, and `📁 Load Audio File...` picker allowing user to load and test any local music file directly without microphone ambient noise.
  4. Retained mathematical synthetic generator (`fillDemo`) as fallback during network buffering, selftest mode (`?selftest=1`), and under the `Synth Tone` button.
  (`tools/serve-web.js`, `web/audio/demo.mp3`, `web/audio/jazz.mp3`, `web/live.js`, `web/main.js`, `web/index.html`, `web/state.js`, `web/style.css`).

- [x] Add a realistic strip look beside the stylized one. `config.look` (persisted, default realistic) switches `StripView` between the glow-and-disc renderer and `web/viz/realistic.js`, which draws each profile's physical `body` in mm: 5050 packages on a black PCB with copper pads, COB phosphor bars, FCOB long per-IC stretches with blended joins, pebble beads on a black cable, silicone sleeve. Placement is unchanged. In realistic mode the glow buffer is hue-preserving (normalised to the brightest channel, not the whitened `expose` output) with an inverse-square falloff, so a red LED blooms red rather than a grey disc. New profiles `fcob`, `peb50`, `peb20`; `placePixels` now returns `at` and `angle`. Follow-ups landed: profiles are now the BTF catalogue by density (WS2812B 30/60/100/144, FCOB 14 to 160 px/m, pebble 2.5 to 20 cm) with one shared 5050 body so width is constant, a Density slider replaces Pitch, an environment glow layer, light room and wood slat surfaces (rod removed), a phone scale floor, and the settings panel docked right of the telemetry above 1100 px. Checked in the browser against the reference photos on synthetic frames only; not yet tuned on live animations, and the FCOB/COB PCB colours, glow radii and pebble bead sizes are eyeballed (`web/viz/realistic.js`, `profiles.js`, `path.js`, `StripView.js`, `hwStore.js`, `web/index.html`, `web/state.js`, `web/viz/CONTEXT.md`, `test/viz/path.test.js`)
- [x] Expose the raw dynamics span's decay in the browser Tuning section. The
  `dynamics signal decay` dial controls the `AudioProcessor` edge-closing
  coefficient independently from the mood classifier's `dynamics window closes`
  rate; setting it to zero keeps a remembered span from collapsing merely because
  analysis frames continue arriving. Native harness coverage and both PlatformIO
  environments pass; hardware remains unverified.
- [x] Expose the audio loudness estimate's gain smoothing in the same Tuning
  section. This is response smoothing, not physical microphone amplification:
  lower values follow input changes faster and higher values retain more of the
  previous estimate.

- [x] Give the browser visualiser a run command and write the conventions around it: `package.json` carries `npm start` over `tools/serve-web.js`, a static server with no dependencies so it works on a bare clone with nothing but Node, and `npm run frames` over `tools/dump-frames.js`, which exists because the harness binary is `program.exe` on Windows and `pio run -t exec` rejects program arguments as stray options. The server is not interchangeable with a plain file server: the module needs `application/wasm` to stream-compile, and nothing under `web/` may be cached while `build-wasm.sh` replaces the module and the recordings under paths already in the browser's cache. `AGENTS.md` is new and holds this repo's stack rules and its three standing prohibitions (`package.json`, `tools/serve-web.js`, `tools/dump-frames.js`, `AGENTS.md`, `README.md`, `.gitignore`)
- [x] Write `ARCHITECTURE.md` and flush the Context it replaced, rather than leaving the durable reasoning inside a working file that gets wiped (`_architecture/ARCHITECTURE.md`, `_architecture/archive/2026-09.md`)
- [ ] Sweep the pipeline for the rest of the defect class the noise floor belonged to: an absolute constant that is really a claim about one input's gain, a reference that is a statistic of the value it is used to judge, a per-block coefficient where a rate per second is meant, a value pinned at 0 or 1 by construction, and state a `resetTracking`-style path misses. Three read-only investigators over `src/audio`, the render path and the stateful reset paths, findings to be verified before any of them is fixed
- [ ] Re-verify the feature scale against the real microphone rather than the synthetic signal, using `docs/glitchglimmer-2026-09-17T20-27-18-691Z.f32`. One 2.3 s capture is all there is, and it is shorter than the windows the measurements now use, so it can show that a value moves but not that it has settled. Replayed through it, `level` reads 0.04 to 0.83 at 2.3 percent churn and is still climbing when the file ends, `mid` reads 0.03 to 0.70 against bass 0.001 to 0.049, and `dynamics` reads 0.00 to 0.26 at 1.5 percent churn. That capture is tonal, so it exercises the noise floor's no-rise path and cannot exercise the rise, which is why the flatness gate is held by a synthetic fixture instead. The tempo's behaviour on live music is still unmeasured, and so is the bass condition on a source with little energy under 200 Hz
- [x] Sweep the plan's Phase B defects. The four inert macros beside `GAIN_SMOOTHING` are deleted, and `GAIN_SMOOTHING` is `0.85f` and now the only statement of that number rather than a `0.92f` reading against a literal. `Animation::begin()` is called at the one moment an animation is constructed, which is what `PsychedelicInkSquirtAnimation` needed before it could take a first-frame delta from `millis()` without integrating the time since boot. The twelve unreachable layers are wired: `LayerType` is eight roles plus twelve concrete values, `addLayerByType` constructs all of them, and the harness sweep grew 164 to 200 checks with every one lighting up and allocating nothing. Both defects the plan named are fixed on the way in (`DominantBandTrailLayer`'s heat array is a member mapped against the strip's own length rather than a shared function-local static mapped against `LED_0_NUM`, and `DynamicsFlickerStormLayer` no longer drives the compositor's opacity from `dynamics`). Two plan items needed no work: the `kMaxLayers` cap is enforced inside `addLayer`, so `applySceneLayers` has nothing to guard, and the hardcoded `5`s are the five ladder rungs, not the mood count. One item is a first cut for the page rather than a tuned result: `SceneDefinition::layerTypes` is chosen by intensity band now instead of being the same two layers on every scene, and which accent belongs on which band is a visual judgement nothing here can settle. Flash 33.0 to 33.4 percent, RAM unchanged (`src/config/Config.h`, `src/scenes/LayerTypes.h`, `src/scenes/LayerManager.cpp`, `src/scenes/SceneRegistry.cpp`, `src/animations/VisualLayers.h`, `src/core/LEDStripController.h`, `src/audio/AudioProcessor.h`, `README.md`)
- [ ] Work the mood arc through to the animations it needs, per `plans/2026-09-18-mood-arc.md`. Steps 2, 3 and 4 are done. Step 3 added an eleventh mood, `DESCENT`, the mirror of `BUILDUP`, which the owner asked for after the plan was written. The enum is the eleven display-order moods plus `MOOD_COUNT`, `classifyMood` is two-stage with the structural conditions answering before the ladder, `moodToString` no longer maps a sentinel onto "Calm", and `pickSceneByMood` is deterministic with no `random()` on the selection path. Both builds and 362 harness checks are green. The detectors live in `AudioProcessor::updateStructure`, exposing `gateGain`, `buildup`, `descent`, `dropDetected`, `spectralFlatness`, `anomaly` and `teaseDetected` on `AudioFeatures`. Two things about them a cold session would get wrong: `gateGain` defaults to `1.0f` rather than `0.0f`, because the harness builds `AudioFeatures` by hand and a zero default would make every fixture read SILENT, and no detector runs while the gate is below `GATE_SETTLED`, because read as a level the gate's own opening is a full-range rise. The rung targets in `SceneRegistry::targetIntensity` are the rung midpoints derived from the ladder's four edges, which is load-bearing: hand-picked values let FLOATY resolve to a brighter scene than CALM, so crossing level 0.20 dropped the scene's intensity while the level rose. Step 5 expanded: 23 Serenity and custom animations were ported and registered into `AnimationCatalog` across the arc (bringing the total to 35 registered animations, with at least 2 to 4 distinct options per mood including dedicated BUILDUP, DROP, WEIRD, TEASE, DESCENT, and SILENT sets). Structural moods now have dedicated candidates and `SceneRegistry::pickSceneByMood` falls back to the catalog if the sole candidate is already running. NoiseWave and Pacifica scaling clear the 64-channel minimum floor check. Flash sits at 35.4% and RAM at 9.3% on `ttgo-t1`. Remaining: record a 60-120 s capture of a real track through the page's Record button into `docs/`, since the 2.3 s one is shorter than every window this now uses and nothing below is tunable without it. Then the ten wired-but-unused layers and the rest of the C2 set. Then the 300-500 ms crossfade and the per-mood frame share in the browser HUD
- [x] Move compositor layers into `src/animations/visual-layers/` so a folder listing is a role listing rather than a mixed dump of bases and accents (`VisualLayer.h`, `VisualLayers.h`, `AlienSquirtTrailLayer.h`)
- [x] Redesign frontend web interface using the `jookoi-vue3-vibe-code` approach. Implemented a "Cockpit" hardware-accurate visual layout using no-build Vue 3 state binding (`main.js`, `state.js`, `index.html`) rather than raw DOM manipulation in `live.js` and `app.js`. Upgraded LED canvas rendering in `render.js` to accurately simulate WS2812b 5050 and COB packages including 144/m, 60/m, fairy light string nodes, and exact pitches. Added intensity sliders and UI presets inside a dedicated `hw-controls` sidebar rather than overlaying the debug area (`web/index.html`, `web/style.css`, `web/render.js`, `web/live.js`, `web/app.js`, `web/main.js`, `web/state.js`).
- [x] Rebuild the browser visualiser on the four-input model — path, LED profile, surface, camera — per `plans/2026-09-18-led-strip-visualizer.md`, built in ten reviewed tasks per `plans/2026-09-18-led-strip-visualizer-build.md`. `web/viz/` holds the renderer behind the unchanged `LedCanvas` contract, so `app.js` and `live.js` were not touched. The page itself was rebuilt in the approved mockup's design language rather than reframed, with the mockup kept at `web/design/mockup-reference.html` as the design source. Three pure modules are tested under `test/viz/` (24 checks on `node --test`, which raised the Node floor to 22 because it no longer accepts a directory argument). Draw time measures 4.2 ms against the 8 ms budget, so the sprite fallback was not needed. What a cold session would otherwise re-derive: continuity is emergent through `fuses()` (`sigma > pitch`) and never a per-type branch, which was caught in review once already; software pixel count is derived from physical length and pitch and sent to the shared WASM library, while fixed device capacity remains a separate limit; and `app.js` and `live.js` each construct their own `StripView` on one canvas, so binding is repeatable and the stale view must be detached or its pointer handlers corrupt the pose. The spectrum, level meter and microphone are confirmed working by the owner; they are not verifiable from a test browser, which blocks the microphone. The pose is saved 1.2 s after a drag settles and is not flushed on unload, which is deliberate — a drag's intermediate positions have no reader and a pose abandoned mid-gesture is not worth rescuing
- [x] Refactor web interface into high-density editor and debugger cockpit. Sits persistent scene, mood state, active compositor layers with individual runtimes, and elapsed scene time directly above the preview canvas in `.cockpit-bar`. Relocated hardware LED strip controls into a slide-away drawer panel with `localStorage` persistence and reset action. Positioned audio telemetry deck (volume, peak, energy, BPM, beat flash, noise floor, gate state, spectral centroid, and vertical meters for bass, mid, treble frequency drives) alongside the preview stage for concurrent visibility. Fixed LED path distribution so changing strip length or scale adapts density without unlit dead tails. Configured mirrored arching strip geometry with center close-up strip. Introspection exported through WASM (`gg_layer_name`, `gg_layer_elapsed_ms`). Documented architecture in `_architecture/VISUALIZER_UI.md` (`_architecture/VISUALIZER_UI.md`, `web/index.html`, `web/style.css`, `web/viz/path.js`, `web/viz/StripView.js`, `web/viz/hwStore.js`, `web/live.js`, `src/wasm_main.cpp`, `src/scenes/LayerManager.h`, `src/core/LEDStripController.h`).
- [x] Stabilize the visualiser controls and cockpit layout. Hardware settings now use physical scale only, with stage width controlling apparent size; every visual range input calls the shared renderer sync, and old saved `fit` values are normalized. The settings drawer enters from the right without a backdrop, while mood, bench readouts, and notes reserve space so changing content cannot reflow later elements. Strip-specific controls also stay unmounted until async renderer binding has populated `hw.strips`; otherwise Vue can render the drawer once with an empty list and throw while reading `lengthM` (`web/index.html`, `web/style.css`, `web/viz/hwStore.js`, `test/viz/hwStore.test.js`).
- [ ] Finish software-controlled strip geometry in the browser. The shared C++ library now separates fixed capacity from runtime software length, WASM exports the setter, and the browser has length/pitch/zoom controls, bounded circular path wrapping, hover handles, and profile visual defaults. The WASM capacity is 12,000 pixels, enough for 20 m at the densest built-in 480/m profile; zoom is an unbounded positive view transform with wheel/pinch/pan navigation and must not change software count or physical length. Remaining verification is the Emscripten build and a served-browser pass; Emscripten is not currently on `PATH`.
- [ ] Enable Pages once in the repository settings, Source set to GitHub Actions, for `.github/workflows/pages.yml` to publish
- [ ] Flash and confirm on hardware: strips animate at all, no crash under scene changes, stable heap, screen free of flicker, mic responsive
