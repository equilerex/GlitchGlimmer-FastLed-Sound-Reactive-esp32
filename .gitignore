# PlatformIO build output
.pio

# Editor and IDE state. Everything here is rewritten per machine or per tool
# version rather than configured for this project. c_cpp_properties.json says so
# itself, and it plus misc.xml embed absolute paths that only resolve on one box.
.idea/
.vscode/c_cpp_properties.json
/.fleet/settings.json
/.serena/

# graphify output, caches and converted sidecars
graphify-out/

# Local harness and build logs
sim-run.txt
*.log
